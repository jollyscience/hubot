Process = require './process'
Client = require '../models/client'

class CreateProject extends Process
	@listensFor: /create[\s]{1,}(a\s+)?(new\s+)?((xero|codebase)\s+)?project/i
	@context: 'create:project'
	
	params:
		project_type: null
		
	needs:[
		Client
	]

	parseParams: (matches) =>
		@params.project_type = 'hey!'

	step: =>

module.exports = CreateProject