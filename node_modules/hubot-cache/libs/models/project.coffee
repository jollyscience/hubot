Model = require './model'

class Project extends Model
  id: null
  namespace: 'Project'

  constructor: (@name) ->

  validation:
    'shortname':
      name: 'shortname'
      message: 'It should be a string between 10 and 14 characters long'
      check: (data) ->
        return data

root = exports ? window
root.Project = Project